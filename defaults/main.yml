---

# 実行コマンド
CMD_LIST:
  - show running-config
  # - show ver

# ログをファイルに保存するか
CREATE_LOG: true

# ログファイルの先頭につける文字列
PREFIX: ''

# ログを残すフォルダ
# LOG_PATH: "{{ role_path + '/log' }}"
LOG_PATH: "{{ lookup('env', 'PWD') + '/log' }}"
